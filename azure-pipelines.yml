# Simple pipeline for testing
trigger:
- feature/allure_oficial

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    echo "Hello World!"
    echo "Current directory:"
    pwd
    echo "Directory contents:"
    ls -la
  displayName: 'Hello World Test'
